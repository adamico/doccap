<tr class=<%= communication.published == "0" ? "danger" : (communication.fichier_url ? nil : "warning")  %>>
  <td><%= link_to communication.titre, communication %></td>
  <td><%= l communication.publication %></td>
  <td><%= communication.category %></td>
  <td><%= communication.tags %></td>
  <td><%= link_to communication.fichier_name, communication.fichier_url, target: "_BLANK" if communication.fichier_url %></td>
  <td>
    <div class="btn-group">
      <%= link_to 'Edit', edit_communication_path(communication), class: "btn btn-warning btn-sm" if can?(:update, communication) %>
      <%= link_to 'Destroy', communication, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-sm" if can?(:destroy, communication) %>
    </div>
  </td>
</tr>
